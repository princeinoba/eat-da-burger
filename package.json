{
  "name": "burgerforge-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first AI burger idea builder, tasting board, and portfolio demonstration.",
  "engines": { "node": "24.x" },
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "verify:build": "node scripts/verify-build.mjs",
    "verify:determinism": "node scripts/verify-determinism.mjs",
    "verify": "npm run lint && npm test && npm run build && npm run verify:build && npm run smoke && npm run verify:determinism",
    "check": "npm run verify"
  }
}
